# /sparklogs-ask

Answer this SparkLogs question: `{{args}}`.

Use the `sparklogs-ask` skill. A few MCP calls. Short precise answer. No investigation template. Offer `/sparklogs-investigate` only if they need a full report.
