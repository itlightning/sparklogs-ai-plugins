---
description: Walk a SparkLogs finding or claim back to the evidence behind it.
argument-hint: <finding or claim to explain>
---

# SparkLogs explain

Explain the evidence behind this SparkLogs finding or claim: $ARGUMENTS

Use the `sparklogs-investigate` skill (if not yet run). Walk through the cited `query_url` (col)s behind the claim and what each one showed, what evidence would refute it, and what was not checked. If the claim lacks evidence, say so and name the query that would supply it. Cause analysis stays in `/sparklogs:analyze-cause`.
