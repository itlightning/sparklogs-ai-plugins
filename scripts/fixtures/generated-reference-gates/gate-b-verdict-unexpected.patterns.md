# Expected patterns: `fixture.module`

Planted positive. The verdict below misfiles an unmatched head.

## How to decide

1. Drop a trailing ` |` if present, then split off the tail after it.
2. Match the longest surface head plus headline below that the pattern starts with. No match means the row is unexpected.
3. The remainder is empty or begins `; ` followed by space-separated tokens.

A reason name carrying a mixed letter-and-digit token is variabilized away before the pattern is
derived, so its rendered pattern matches no head. Under the rule above that harmless shape files
as drift on every run.
