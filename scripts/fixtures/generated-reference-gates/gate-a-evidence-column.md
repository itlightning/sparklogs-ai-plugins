# Field schema: `fixture.module`

Planted positive. This table carries the evidence axes that stay inside the source library.

| LQL path | Type | Evidence | Meaning |
|---|---|---|---|
| `fixture.module.example_code` | string | spec | A decoded example that exists only to give the gate something to catch. |

| Code | Token | Meaning | Spec | Observed | Constant |
|---|---|---|---|---|---|
| `0x1` | `example_token` | an example meaning | yes | no | `EXAMPLE_CONSTANT` |
