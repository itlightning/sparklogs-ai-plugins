# Vocabularies: `fixture.module`

Planted positive. The paragraphs below explain the internal measuring instrument.

## Evidence tiers

Two independent evidence axes per row, because they answer different questions.

## Witness corpus

Witness counts come from replayed endpoint captures, so a row with no witnesses is unwitnessed
rather than wrong. A code the corpus never produced reads as not counted.
